Claude Code should follow the repository-wide operating rules and project
context in `AGENTS.md`. See `AGENTS.md` for full project instructions,
including the static-site architecture, validation commands, directory map,
generated-file rules, and brand-specific governance.
