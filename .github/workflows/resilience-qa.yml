name: Resilience QA

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: resilience-qa-${{ github.ref }}
  cancel-in-progress: true

jobs:
  resilience-qa:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    name: Resilient web behavior (Chromium, Firefox, WebKit)

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.11'

      - name: Install Playwright
        run: pip install playwright

      - name: Install browser engines and system dependencies
        run: playwright install --with-deps chromium firefox webkit

      - name: Start local HTTP server
        run: python3 scripts/serve-site.py > /tmp/resilience-server.log 2>&1 &

      - name: Generate the same universe inventory as publication
        run: |
          python3 scripts/build-search-index.py
          python3 scripts/sync-portfolio-stats.py
          python3 scripts/build-search-index.py
          python3 scripts/sync-css-version.py

      - name: Wait for server to be ready
        run: |
          for i in $(seq 1 30); do
            curl -sf http://localhost:5000/ > /dev/null && echo "Server ready." && break
            sleep 0.5
          done
          curl -sf http://localhost:5000/ > /dev/null \
            || { echo "ERROR: server did not start in time"; cat /tmp/resilience-server.log; exit 1; }

      - name: Run resilient web behavior acceptance checks
        run: python3 scripts/resilience-qa.py --base-url http://localhost:5000

      - name: Run color-scheme bootstrap regression in all installed engines
        run: |
          failed=0
          for browser in chromium firefox webkit; do
            python3 scripts/tests/test-color-scheme-init.py \
              --base-url http://localhost:5000 \
              --browser "$browser" \
              --output "assets/audit/color-scheme-init-$browser.json" \
              || failed=1
          done
          exit "$failed"

      - name: Verify comparable first-paint evidence
        run: |
          python3 - <<'PY'
          import json
          from pathlib import Path

          audit_dir = Path("assets/audit")
          expected_browsers = ("chromium", "firefox", "webkit")
          allowed_statuses = {
              "observed",
              "unsupported",
              "unavailable_after_load",
          }
          invalid = []
          for browser in expected_browsers:
              path = audit_dir / f"color-scheme-init-{browser}.json"
              try:
                  report = json.loads(path.read_text(encoding="utf-8"))
              except (OSError, json.JSONDecodeError) as error:
                  invalid.append(f"{path.name}: cannot parse JSON ({error})")
                  continue
              if report.get("browser") != browser:
                  invalid.append(
                      f"{path.name}: browser={report.get('browser')!r}, "
                      f"expected {browser!r}"
                  )
              if report.get("status") != "PASS":
                  invalid.append(
                      f"{path.name}: status={report.get('status')!r}, expected 'PASS'"
                  )
              cases = report.get("cases")
              if not isinstance(cases, dict):
                  invalid.append(f"{path.name}: cases must be an object")
                  continue
              for case_name, case in cases.items():
                  for route_evidence in case.get("route_evidence", []):
                      timing = route_evidence.get("timing", {})
                      status = timing.get("first_paint_status")
                      if status not in allowed_statuses:
                          invalid.append(
                              f"{path.name}: {case_name}/"
                              f"{route_evidence.get('route')}: "
                              f"invalid first_paint_status={status!r}"
                          )

          if invalid:
              raise SystemExit(
                  "First-paint evidence is incomplete or not comparable: "
                  + "; ".join(invalid)
              )
          print(
              "Verified first-paint evidence status values for chromium, "
              "firefox, and webkit."
          )
          PY

      - name: Verify generated maps in all installed engines
        run: python3 scripts/tests/test-universe-browser.py

      - name: Run native visitor acceptance in all installed engines
        run: |
          failed=0
          for browser in chromium firefox webkit; do
            python3 scripts/tests/test_browser_acceptance.py \
              --base-url http://localhost:5000 --browser "$browser" \
              --output "assets/audit/browser-acceptance-$browser.json" || failed=1
          done
          exit "$failed"

      - name: Capture resilience diagnostics
        if: always()
        run: |
          {
            echo "=== Python ==="
            python3 --version || true
            echo "=== Playwright ==="
            python3 -m playwright --version || true
            echo "=== Playwright browser cache ==="
            find "${PLAYWRIGHT_BROWSERS_PATH:-$HOME/.cache/ms-playwright}" \
              -maxdepth 2 -type f -name 'INSTALLATION_COMPLETE' -print 2>/dev/null || true
            echo "=== Browser process inventory ==="
              ps -eo pid=,comm= | grep -E 'firefox|webkit|chromium' || true
            echo "=== Server log ==="
            cat /tmp/resilience-server.log 2>/dev/null || true
          } > /tmp/resilience-qa-diagnostics.txt 2>&1

      - name: Upload resilience diagnostics
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: resilience-qa-diagnostics
          path: /tmp/resilience-qa-diagnostics.txt
          if-no-files-found: warn
          retention-days: 30

      - name: Upload resilience report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: resilience-qa-report
          path: |
            assets/audit/resilience-qa-*.json
            assets/audit/browser-acceptance-*.json
            assets/audit/color-scheme-init-*.json
          if-no-files-found: error
          retention-days: 30
