# Local pull-request draft

Prepared locally for the reviewed corrective branch against main. No external PR has been created. Exact commits and local artifact evidence are recorded in `pm-integration-record.md`; release authorization and GitHub checks remain separate.

## Title

Correct catalog discovery, accessibility, claims, and release packaging

## Body

Visitors could encounter the wrong search identity, unavailable GPT launch routes, inaccurate saved analytics status, unreadable dark-mode text, and keyboard focus that stayed in the header or failed to return from search. The branch corrects these paths while preserving the seven branches, 42 Tool-ettes, 60 indexable pages, and existing 1 live / 24 beta / 17 unavailable publication states.

Search now shares useful content indexing, ranking, category and publication labels, history, retry, and keyboard behavior. Its loading space is reserved to reduce content movement. The homepage selects an existing hero image appropriate to its rendered size and lowers the priority of the below-fold game image. Scoped contrast and focus corrections preserve the existing brand and sibling search behavior.

The catalog describes owned pages and intended uses without unsupported model, memory, routing, pricing, or screenshot assertions. All 42 detail schemas use canonical WebPage identity; the Showcase and 38 advisory findings are reconciled with source evidence. Sitemap dates reflect inspected significant changes, with three optional dates omitted where history did not support them. The historical feed is unchanged pending its documented maintenance decision.

The service worker preserves valid online responses when cache storage fails and bounds query caching. Pages packaging uses an explicit public inventory, including the hidden public security files, and verifies bytes and provenance before upload, after download, and inside the final Pages tar. Active runbooks and the release packet distinguish local checks, observed GitHub settings, proposed policy, and deployment evidence.

Validation:

- Structural validation: 63 pages, zero issues/warnings. Internal links: zero broken, 60 sitemap URLs. Advisory audit: zero findings. Generated index/statistics/CSP/cache tokens, strict contrast, and both brand coverage checks pass.
- Unit discovery: 54 tests, 53 pass and one Windows symlink privilege skip. Additional visitor, search producer, reconciliation, hook, client, and service-worker regressions pass.
- Actual Chromium sweep: 504/504 page-width cases pass. Independent focused checks: 26 Chromium/WebKit, 13 rendered, and 11 search/consent journeys pass. Delayed diagram, image decode, keyboard, reflow, reduced-motion, and forced-color checks have separate dated evidence.
- Local performance evidence supports the search-space correction and 100,926 fewer hero image bytes at desktop DPR1. One homepage layout-shift outlier remains documented; six isolated follow-up cases did not reproduce it. No field-performance claim is made.
- Python browser CI, actual GitHub artifact transfer, deployment, and live provenance/routes/headers remain pending. Installed Firefox fails before page creation; WebKit next-link Tab, true browser zoom, human assistive technology, and loaded Google Fonts have explicit local coverage limits.

The broader catalog prototype and feed, dependency, hosting, review-policy, and sibling-promotion proposals are reviewable artifacts. Their delivery does not authorize production rollout, upgrades, settings, synchronization, or publication. See the assignment register and platform handoff for each outstanding decision and release gate.
