# Structured Data Audit — 2026-05-28
## Google Rich Results Test — SoftwareApplication Compliance

### Scope
Four illustrated tool-ette pages verified against Google's Rich Results requirements for
SoftwareApplication, Breadcrumb, and Schema.org best practices:

| Page | Path |
|---|---|
| Career Seeker | `toolbox/01-discovered-careers/01f-career-seeker/` |
| Menu Conductor | `toolbox/03-tasty-tracker/03b-menu-conductor/` |
| Present Hoarder | `toolbox/02-treasured-finds/02c-present-hoarder/` |
| Memento Log | `toolbox/04-travelers-guide/04e-memento-log/` |

---

### Google Rich Results — SoftwareApplication Requirements

| Requirement | Status | Notes |
|---|---|---|
| `name` | ✅ Present on all 4 | Derived from `<title>` |
| `operatingSystem` | ✅ Fixed → `"Web"` | Was `"Any (web)"` — non-standard; `"Web"` is the clean Schema.org value |
| `applicationCategory` | ✅ `"ProductivityApplication"` | Valid Schema.org enum value |
| `offers` (price data) | ✅ `price: "0", priceCurrency: "USD"` | Satisfies the `offers` OR `aggregateRating` requirement |
| `image` caption quality | ✅ Fixed — all 4 descriptive | `present-hoarder` and `memento-log` had stale `"— illustration"` captions; now populated from updated `og:image:alt` |

---

### Structural Issues Found and Fixed

#### Issue 1 — BreadcrumbList nested inside SoftwareApplication (high impact)

**Before:**
```json
{
  "@graph": [
    {
      "@type": "SoftwareApplication",
      "breadcrumb": {
        "@type": "BreadcrumbList",
        "itemListElement": [...]
      }
    }
  ]
}
```

**Problem:** Nesting the BreadcrumbList prevents Google's Rich Results parser from
evaluating it as an independent Breadcrumb rich result. A nested entity cannot appear
as a top-level rich result type in the SERP.

**After (fixed in `scripts/inject-jsonld.py`):**
```json
{
  "@graph": [
    {
      "@type": "SoftwareApplication",
      "breadcrumb": { "@id": "https://glee-fully.tools/toolbox/.../#breadcrumb" }
    },
    {
      "@type": "BreadcrumbList",
      "@id": "https://glee-fully.tools/toolbox/.../#breadcrumb",
      "itemListElement": [...]
    }
  ]
}
```

BreadcrumbList is now a first-class `@graph` node with its own `@id`.
SoftwareApplication references it by `@id` — valid linked-data pattern.
This enables Google to parse **both** a SoftwareApplication rich result **and** a
Breadcrumb rich result from the same page.

**Scope:** All 42 tool-ette pages, 8 branch-hub pages, 8 supporting pages — 57 pages total,
regenerated by re-running `scripts/inject-jsonld.py`.

---

#### Issue 2 — `operatingSystem: "Any (web)"` (low impact)

**Before:** `"operatingSystem": "Any (web)"`

**Problem:** Non-standard free-form string. Google's documentation examples use recognized
OS names or `"Web"` for browser-based apps.

**After:** `"operatingSystem": "Web"`

---

#### Issue 3 — Stale image captions on 2 pages (medium impact)

**Pages:** `02c-present-hoarder`, `04e-memento-log`

**Before:** `"caption": "Present Hoarder — illustration"` / `"caption": "Memento Log — illustration"`

**Root cause:** `inject-jsonld.py` derives `image.caption` from `og:image:alt`. Both pages
had their `og:image:alt` updated with descriptive text but `inject-jsonld.py` had not
been re-run since that update.

**After:** Both captions are now fully descriptive (pulled from current `og:image:alt`):
- Present Hoarder: *"Glee-fully Present Hoarder — retro-bright gift wishlist organiser showing people-organised lists with budget tracking, occasion tags, and purchase status badges"*
- Memento Log: *"Glee-fully Memento Log — retro-bright travel keepsake journal showing a scrapbook layout with souvenir tags, photo placeholders, ticket stubs and memory captions"*

---

### Remaining Advisory Items (not blocking rich-result eligibility)

| Item | Severity | Notes |
|---|---|---|
| `image` uses SVG format | Advisory | Google recommends JPEG/PNG for rich-result thumbnails; SVG is valid but may not render as a preview image in SERPs |
| No `aggregateRating` | Advisory | Adding real user ratings would unlock star-rating display in SERPs; deferred until a rating mechanism exists |
| No `screenshot` property | Advisory | Google's SoftwareApplication docs list `screenshot` as recommended; currently served via `image` only |

---

### Exit State

| Check | Result |
|---|---|
| `python3 scripts/validate-site.py` | 61/61 pages — 0 issues, 0 warnings |
| `python3 scripts/check-links.py` | 0 broken links, 2,413 internal links, 59/59 sitemap URLs |
| JSON-LD parses cleanly | ✅ All 57 pages |
| @graph structure: SoftwareApplication + BreadcrumbList top-level | ✅ All 42 tool-ette pages |
| BreadcrumbList `@id` resolvable within same `@graph` | ✅ Verified on all 4 target pages |
| Required SoftwareApplication fields present | ✅ name, operatingSystem, applicationCategory, offers |
